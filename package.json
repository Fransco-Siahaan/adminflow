{
  "name": "adminflow",
  "version": "1.0.0",
  "private": true,
  "description": "AdminFlow — Simple Administrative Workflow Management System",
  "scripts": {
    "build": "cd server && npm install --include=dev && npm run build",
    "start": "cd server && npm start"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}